{% extends '_modal.html.twig' %}

{% block modal %}
{{ form_start(form, {
    'method': 'PUT',
    'action': path('user_edit_address', {id: address.id})
}) }}
    {{ form_widget(form) }}
    <input type="submit" value="Modifier">
{{ form_end(form) }}
{% endblock %}