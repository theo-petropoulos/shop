{% extends 'base.html.twig' %}

{% block pretitle %}INSCRIPTION{% endblock %}

{% block body %}
    <main id="inscription">
        <div id="inscription_header">
            <h3>INSCRIPTION</h3>
            <h4>Déjà inscrit ? <a href="{{ path('user_login') }}">CONNEXION</a></h4>
        </div>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Valider">
        {{ form_end(form) }}
    </main>
{% endblock %}