{% extends 'base.html.twig' %}

{% block pretitle %}RÉINITIALISATION DU MOT DE PASSE{% endblock %}

{% block body %}
    <main id="reset_password">
        <div id="reset_password_header">
            <h3>Nouveau mot de passe</h3>
        </div>
        {{ form_start(resetForm) }}
            {{ form_widget(resetForm) }}
            <input type="submit" value="Valider">
        {{ form_end(resetForm) }}

        <a class="back-btn" href="{{ path('home') }}">Annuler</a>
    </main>
{% endblock %}
